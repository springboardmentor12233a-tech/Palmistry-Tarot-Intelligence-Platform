# --- Platform (API + frontend + AI interpretation) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
streamlit==1.38.0
requests==2.32.3
reportlab==5.0.0
google-genai==0.3.0
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2
python-dotenv==1.0.1

# --- Palmistry pipeline (palmistry-main/code) ---
# Left unpinned to match the original repo's requirements.txt — pin these
# once you've confirmed exact working versions, since mediapipe in
# particular has had breaking API changes across releases and needs a
# Python version it explicitly supports (check before bumping Python).
torch
torchvision
scikit-image
opencv-python-headless
pillow-heif
mediapipe==0.10.9
# ^ pinned deliberately — 0.10.10+ broke the legacy `mp.solutions.*` API
# (hands, face mesh, etc.) that rectification.py and measurement.py use.
# Google has confirmed this is a permanent deprecation, not a bug that
# will be fixed: https://github.com/google-ai-edge/mediapipe/issues/5168
# 0.10.9 is the last version where it works. Don't bump this without
# rewriting rectification.py/measurement.py to the new Tasks API first.matplotlib
